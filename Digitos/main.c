#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num,i;
    char digitos[100];

    printf("Ingrese Numero: ",200);
    scanf("%d",&num);

    for(num=0;digitos[num]!='0';num++);

        for(i=0;i<num;i++);

            printf("%d",digitos[i]);


    return 0;

}
