#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i;
    int num[2];

    for(i=0;i<1;i++)
    {
        printf("Ingrese numero: ");
        scanf("%d",&num[i]);
        for(i=0;i<1;i++)
        {
        printf("%d ",num[i]);
        }
    }
    return 0;
}
